# This is a basic workflow to help you get started with Actions

name: vcpkg

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:


jobs:
  prepare-env:
    # The type of runner that the job will run on
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      # Runs a set of commands using the runners shell
      - name: prepare conda environments
        run: |
          C:\Miniconda\bin\conda create --yes -p C:\conda_envs\cpp_pkgs -c conda-forge python=3.9
          C:\Miniconda\bin\conda activate cpp_pkgs
          conda install --yes -c conda-forge boost-cpp eigen nlohmann_json mkl mkl-devel

